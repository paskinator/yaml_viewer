{
  "name": "yaml-table-viewer",
  "displayName": "YAML Table Viewer (Editable with Nested Support)",
  "description": "Preview, filter, add, edit and save YAML 'tables:' as an editable table with nested objects support.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.80.0"
  },
  "activationEvents": [
    "onCommand:yamlTableViewer.openPreview"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "yamlTableViewer.openPreview",
        "title": "Open YAML Table Preview (Editable)"
      }
    ]
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
